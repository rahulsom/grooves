apply plugin: "jacoco"

jacoco {
    toolVersion = '0.8.12'
}

project.tasks.withType(Test).configureEach {
    it.finalizedBy("jacocoTestReport")
    it.jacoco.destinationFile = file("$buildDir/jacoco/test.exec")
}
