apply plugin: "jacoco"

jacoco {
    toolVersion = '0.8.14'
}

project.tasks.withType(Test).configureEach {
    it.finalizedBy("jacocoTestReport")
    it.jacoco.destinationFile = file("${layout.buildDirectory.get().asFile}/jacoco/test.exec")
}
