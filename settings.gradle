rootProject.name = 'grooves'

def projects = ['api', 'groovy', 'gorm',]
def examples = ['test', 'springboot-jpa', 'grails-rdbms', 'grails-mongo',]

projects.each { include "grooves-${it}" }
examples.each { include "grooves-example-${it}" }
